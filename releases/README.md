# Releases

Each release of CARD CATALOG is saved here as its own file, named:

    card-catalog-vX.Y.Z.html

Release builds may inline `cardcatalog-core.js` to pursue the single-file goal. The test harness in `tests.html` must pass in full before any file lands in this directory.
